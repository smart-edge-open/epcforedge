# `/test/oam/cnca-cli-scripts`

- Test apps and test data for simulating cnca.
- Can use `./runAll.sh` to run all methods testing automatically.
- Can also use `./cliTest.sh`to run different method manually. 
